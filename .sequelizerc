const path = require("path");

const DB_ROOT_DIR = "src/backend/database";

module.exports = {
  config: path.resolve(DB_ROOT_DIR, "config", "config.js"),
  "models-path": path.resolve(DB_ROOT_DIR, "models"),
  "seeders-path": path.resolve(DB_ROOT_DIR, "seeders"),
  "migrations-path": path.resolve(DB_ROOT_DIR, "migrations")
};
